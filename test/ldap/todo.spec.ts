import {jest,describe,expect,test, beforeAll, afterAll} from "@jest/globals";

describe("Testes plugin better auth ldap",() => {
    test("Falta implementar os testes!", async () => {

        expect(1 + 1).toBe(2);
    });
});